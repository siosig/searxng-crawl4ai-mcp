{
  "mcpServers": {
    "searxng-crawl4ai": {
      "type": "http",
      "url": "${user_config.endpoint}",
      "headers": {
        "Authorization": "Bearer ${user_config.token}"
      }
    }
  }
}
